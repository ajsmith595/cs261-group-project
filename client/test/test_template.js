const fetch = require("node-fetch");
const {
    SERVER_HOST,
    sleep,
    getWebSocketPromise,
    sleepMs,
} = require("./common");
/**
 * Test template
 * @param {Array} users
 */
module.exports = async function test_template(users) {};
